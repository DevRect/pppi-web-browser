#include "CookieManager.h"

CookieManager::CookieManager(QObject* parent) : QObject(parent) {}

void CookieManager::clearCookies() {
    // Логика очистки
}

void CookieManager::setCookie(const QString& key, const QString& value) {
    // Логика установки куки
}